name: CI

on:
  push:
    branches:
      - main
      - master
      - dev
      - staging
      - production
  pull_request:
    branches:
      - main
      - master
      - dev
      - staging
      - production

jobs:
  check:
    name: Lint, typecheck and build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run checks
        run: npm run check

  environment-build:
    name: Build ${{ github.ref_name }} environment
    runs-on: ubuntu-latest
    needs: check
    if: github.event_name == 'push' && contains(fromJSON('["dev", "staging", "production"]'), github.ref_name)
    environment:
      name: ${{ github.ref_name }}
    env:
      VITE_APP_ENV: ${{ vars.VITE_APP_ENV }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate environment variables
        run: |
          if [ -z "$VITE_APP_ENV" ]; then
            echo "VITE_APP_ENV is required for the ${{ github.ref_name }} environment."
            exit 1
          fi

          echo "Building for environment: $VITE_APP_ENV"

      - name: Build application
        run: npm run build
